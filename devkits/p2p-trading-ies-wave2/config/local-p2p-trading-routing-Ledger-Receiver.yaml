# Ledger Receiver routing: incoming beckn calls → sandbox-ledger
routingRules:
  - version: "2.0.0"
    targetType: "url"
    target:
      url: "http://sandbox-ledger:3003/api/bpp-webhook"
    endpoints:
      - on_confirm
      - status
      - on_status
